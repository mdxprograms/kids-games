export const imgStyles = color => ({
  border: `4px solid ${color}`,
  width: "10vw"
});

export const textStyles = color => ({
  background: color,
  color: "#fff",
  margin: "20px 0 0",
  padding: "10px 0"
});
